import UIKit

var str = "Hello, playground"

func strStr(_ haystack: String, _ needle: String) -> Int {
    let a = Character(needle)
    let b = haystack.firstIndex(of: a)
    String.Index
    print(b.hashValue)
    return 0
}

strStr("aaaba", "b")
